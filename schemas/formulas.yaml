type: enum('package-manager', 'custom')
os: enum('linux', 'windows', 'darwin', 'ubuntu', 'any')
shell:  enum('cmd', 'powershell', required=False)
commands:
  init: str(required=False)
  install: str(required=False)
  install-global: str(required=False)
  uninstall: str(required=False)
  update-all: str(required=False)
requires: list(str(), required=False)
