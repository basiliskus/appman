os:
  - windows
  - unix-like:
    - linux:
      - ubuntu
    - darwin
defaults:
  pm:
    cli:
      windows: [ scoop, choco, npm, pip, cargo ]
      ubuntu: [ apt, npm, pip, cargo, snap ]
      darwin: [ brew, npm, pip, cargo ]
    gui:
      windows: [ choco ]
      ubuntu: [ apt, snap ]
      darwin: [ brew ]
    backend:
      any: [ npm, pip, cargo ]
    fonts:
      windows: [ scoop ]
      ubuntu: []
      darwin: [ brew ]
    drivers:
      any: []
    vscode:
      any: [ vscode-extensions ]
    provisioned:
      windows: [ appx ]
      ubuntu: [ apt ]
  tags:
    - dev
    - essentials
    - utils
