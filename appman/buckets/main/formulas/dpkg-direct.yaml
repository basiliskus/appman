type: custom
os: ubuntu
commands:
  install: >-
    EXT='.deb'
    && NAME=$(basename $source ${EXT})
    && wget -q -O ~/tmp/${NAME}${EXT} $source
    && sudo dpkg -i ~/tmp/${NAME}${EXT}
    && sudo apt-get install -f
requires:
  - wget
