os: any
commands:
  init: npm install --global npm
  install: npm install --global $pmid
  uninstall: npm uninstall --global $pmid
  update-all: npm update --global
  installed: npm ls --global $pmid
requires:
  - npm
