type: custom
os: ubuntu
commands:
  install: >-
    EXT='.deb'
    && NAME=$(basename $source ${EXT})
    && wget -q -O -x ~/tmp/${NAME}${EXT} $source
    && sudo apt-get install -y ~/tmp/${NAME}${EXT}
requires:
  - wget
