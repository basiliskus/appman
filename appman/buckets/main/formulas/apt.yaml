os: ubuntu
commands:
  init: >-
    sudo apt-get update -qq
    && sudo apt-get upgrade -qq --yes
  install: sudo apt-get install -qq --yes $pmid
  uninstall: sudo apt-get remove -qq --yes $pmid
  update-all: sudo apt-get upgrade -qq --yes
  installed: apt list --installed -qq $pmid
