os: windows
commands:
  install: winget install --silent --exact --id $pmid
  uninstall: winget uninstall --silent --exact --id $pmid
  update-all: winget upgrade --all
  installed: winget list $pmid
requires:
  - winget
