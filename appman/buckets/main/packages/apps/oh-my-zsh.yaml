id: oh-my-zsh
name: oh-my-zsh
labels:
  - cli
custom-formulas:
  - os: linux
    commands:
      install: sh -s -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"
      uninstall: uninstall_oh_my_zsh
    requires:
      - curl
